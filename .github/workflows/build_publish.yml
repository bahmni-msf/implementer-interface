name: Build and Publish
on:
  push:
    branches:
      - githubActions
  workflow_dispatch:


jobs:
  docker-build-publish:
    name: Docker Build & Publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set env.ARTIFACT_VERSION
        run: |
          wget -q https://raw.githubusercontent.com/Bahmni/bahmni-infra-utils/main/setArtifactVersion.sh && chmod +x setArtifactVersion.sh
          ./setArtifactVersion.sh
          rm setArtifactVersion.sh
      # - name: Use Node.js 12
      #   uses: actions/setup-node@v1
      #   with:
      #     node-version: "12.x"
      # - name: Install dependencies
      #   run: yarn install --frozen-lockfile
      # - name: Run tests
      #   run: yarn test
      # - name: Package
      #   run: yarn ci
      # - name: Build Docker image
      #   run: |
      #     docker build -t $ECR_REGISTRY/$IMAGE_NAME:latest .
      #     docker tag $ECR_REGISTRY/$IMAGE_NAME:latest $ECR_REGISTRY/$IMAGE_NAME:$GITHUB_SHA
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.BAHMNI_LITE_AWS_ACCESS_KEY }}
          aws-secret-access-key: "${{ secrets.BAHMNI_LITE_AWS_SECRET_KEY }}ddd"
          aws-region: ap-south-1
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1
      # - name: Login to DockerHub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_HUB_USERNAME }}
      #     password: ${{ secrets.DOCKER_HUB_TOKEN }}
      # - name: Docker Build and push
      #   uses: docker/build-push-action@v3
      #   with:
      #     context: .
      #     platforms: linux/amd64,linux/arm64
      #     file: package/docker/Dockerfile
      #     push: true
      #     tags: bahmni-msf-lite/implementer-interface:${{env.ARTIFACT_VERSION}},bahmni-msf-lite/implementer-interface:latest